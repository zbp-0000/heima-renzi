import Layout from '@/layout'
export default {
  path: '/{{ name }}',
  component: Layout,
  children: [{
    // path: '' 默认路由
    // 当访问/{{ name }}默认访问到页面
    path: '',
    name: '{{ name }}',
    component: () => import('@/views/{{ name }}/index.vue'),
    meta: { title: '{{ title }}', icon: '' }
  }]
}
